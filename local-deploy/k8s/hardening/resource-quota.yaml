apiVersion: v1
kind: ResourceQuota
metadata:
  name: graph-olap-quota
  labels:
    app.kubernetes.io/part-of: graph-olap-platform
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 16Gi
    limits.cpu: "16"
    limits.memory: 32Gi
    pods: "50"
